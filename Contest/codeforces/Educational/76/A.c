#include <stdio.h>
#include <math.h>

int main () {
    // Declaration
    int t;                  // number of test cases
    int n;                  // number of the total students
    int x;                  // number of the swaps
    int a;                  // position of the first student
    int b;                  // position of the second student
    int i;                  // for iteration, as usual
    int minPos;             // finding the minimum position of a and b

    // Input
    scanf("%d", &t);

    for (i = 0; i < t; i++) {
        scanf("%d %d %d %d", &n, &x, &a, &b);
        if (a <= b) {
            if (abs(a)) {
                
            }
        }
    }
}